#ifndef VOXELENGINE_UTILS_H
#define VOXELENGINE_UTILS_H

#include <GL/glew.h>
#include <GLUT/glut.h>
#include <string>

void drawText(const std::string &text, float x, float y, float r, float g, float b);

#endif //VOXELENGINE_UTILS_H
